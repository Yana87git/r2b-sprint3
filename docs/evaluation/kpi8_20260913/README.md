# 評価の証跡（2026-09-13）

D2 の8件（KPI8・KPI4）と、D1 の成功した実行1本。**実行して出たものをそのまま置いてある**。
作り直したいときは `backend/scripts/eval_d2.py`（読み取り）と `backend/scripts/verify_kpi4.py`
（修正して確定）で同じものが出る。

## 品目リスト（Excel）8件 — KPI4「見逃し0件」の現物

仕込んだ誤りを画面と同じ API で直し、確定して出力したもの。**8件とも、仕込んだ誤りが
出力に残っていない**（正解表は `backend/tests/fixtures/README.md`「D2 の正解表」）。

| ファイル | 仕込んだ誤り | 人がした操作 | 出力 |
|---|---|---|---|
| C1_見積依頼_東亜精機.xlsx | 型番 `BRG-62O5-2RS`（0 が英字 O） | 型番を `BRG-6205-2RS` に直す | 誤りなし・5行 |
| C2_RFQ_Hokuriku.xlsx | 六角穴付きボルトの単位 `m` | 単位を `本` に直す | 単位 m の行なし・4行 |
| C3_見積依頼_中央テクノ.xlsx | 型番 `OSL-l520-N`（1 が小文字 l） | 型番を `OSL-1520-N` に直す | 誤りなし・5行 |
| C4_見積依頼_西日本機工.xlsx | 数量 `2000`（他の行は 4〜8） | 数量を**要確認**にした（正しい数量は書類から決まらない） | 2000 なし・顧客回答待ち1行 |
| C5_部品手配依頼_大和バルブ.xlsx | 型番だけ全角 `ＨＢＴ－Ｍ１０－４０` | 正規化後の値（半角）を確認して保存 | 全角の型番なし・4行 |
| C6_新星商会_メール本文.xlsx | Oリングの単位 `kg` | 単位を `個` に直す | 単位 kg の行なし・4行 |
| C7_三葉工業_Excel＋PDF.xlsx | `PPN-8-40` の数量が Excel 40 / PDF 400 | **両方の行の数量を要確認**にした | どちらの数字も確定値として出ていない・顧客回答待ち2行 |
| C8_東海精密_Word＋メール本文.xlsx | `ACY-SD40-150` の納期が 10/31 と 11/30 | **両方の行の納期を要確認**にした | 同上・顧客回答待ち2行 |

**C7・C8 の判断**: どちらが正しいかは書類から決まらないので、片方を選ばず、両方の行を
残したまま食い違っている項目だけを「要確認（顧客回答待ち）」にした。確かでない値を
確定した値として外に出さない（① ビジネスゴール2）。

## trace_d1_292d91f0.jsonl — D1 の成功した実行（⑥ 3章の5観点）

`backend/traces/` は git 管理外なので、代表を1本だけここに写している。

| 観点 | このトレースで見えること |
|---|---|
| 完了条件 | `check_completion` が最後に1回だけ呼ばれ、`{"result":"completed","unmet":[]}` で終わっている |
| ツールの使い方 | `list_input_files` → `read_file_content`×2 → `save_item_rows` → `verify_sources` → `check_completion`。**`agent.md` のツール一覧の6つの範囲内**で、表にないツールは出てこない |
| ガードレール | 組み込みツール（Bash・Read・Write 等）の呼び出しが1件も無い（`disallowed_tools` でブロック）。案件IDはツールの引数に現れない（サーバー側で注入している） |
| 停止条件 | `run_end` の `stop_reason` が `completed`。7ターン（上限30）・タイムアウトは発火していない |
| 秘密情報 | `read_file_content` の観察は**要約だけ**（ファイル名・範囲・行数）で、**原本の本文は残っていない**。宛先・発行元・担当者名（佐々木・東洋テクノ工業など）も出てこない。API キーも無い。**ただし `save_item_rows` の引数には抽出した品目の値（原文を含む）が残る** — 何をどう読み取ったかを評価するために必要なので、これは意図どおり |
