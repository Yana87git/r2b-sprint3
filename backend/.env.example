# Database（Slice 0-2 の docker compose と合わせる）
DATABASE_URL=postgresql+psycopg://r2b:r2b_local_dev@localhost:5432/inquiry

# Application
DEBUG=True
APP_NAME=引合書整理エージェント API

# CORS（Next.js 15 / ポート 3000）
ALLOWED_ORIGINS=["http://localhost:3000"]

# エージェント（実際の値は backend/.env にのみ置く。git 管理外）
ANTHROPIC_API_KEY=sk-ant-...

# 認証は今回未実装（① 6章）。Scope 2 で JWT を入れるときに使う
# JWT_SECRET_KEY=
# JWT_ALGORITHM=HS256
